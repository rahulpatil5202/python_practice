import random

print(random.randint(0,100)) #generating random number from 0 to 100
print(random.randint(0,100)) #generating random number from 0 to 100
print(random.randint(0,100)) #generating random number from 0 to 100
print(random.randint(0,100)) #generating random number from 0 to 100